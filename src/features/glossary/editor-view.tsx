import type { ProjectView } from "@/types/generated/ProjectView";
import type { GlossaryDoc } from "@/types/generated/GlossaryDoc";

// Replaced with the real editor in the next task.
export function EditorView(_props: { view: ProjectView; doc: GlossaryDoc }) {
  return null;
}
